{
  "name": "org-ietf-argon2",
  "private": true,
  "description": "RFC 9106 Argon2 in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs"
  }
}
